'use client'

import TryMode from '@/components/TryMode'

export default function Home() {
  // Landing page - just shows TryMode
  // Navigation to other pages is handled by TryMode component
  return <TryMode />
}

